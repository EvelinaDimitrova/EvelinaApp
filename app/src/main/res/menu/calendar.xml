<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_today"
        android:title="Today"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"/>
    <group android:checkableBehavior="single">
        <item android:id="@+id/action_day_view"
            android:title="day_view"
            android:orderInCategory="100"
            app:showAsAction="never"/>
        <item android:id="@+id/action_three_day_view"
            android:title="three_day_view"
            android:orderInCategory="100"
            app:showAsAction="never"
            android:checked="true"/>
        <item android:id="@+id/action_week_view"
            android:title="week_view"
            android:orderInCategory="100"
            app:showAsAction="never"/>
    </group>
    <group android:checkableBehavior="all">
        <item android:id="@+id/cbShowScheduledEvents"
            android:title="Show Schedule"
            android:orderInCategory="100"
            android:checked="true"
            app:showAsAction="never"/>
        <item android:id="@+id/cbShowEvents"
            android:title="Show Event"
            android:orderInCategory="100"
            android:checked="false"
            app:showAsAction="never"/>
    </group>

</menu>
